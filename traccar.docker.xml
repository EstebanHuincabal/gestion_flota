<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM 'http://java.sun.com/dtd/properties.dtd'>
<properties>

    <!-- Config de DESARROLLO inyectada con `docker cp` (el backend Django corre
         en el host, fuera de Docker). Conserva la base H2 de la imagen y agrega
         el reenvío de posiciones hacia el Django local. -->

    <entry key='database.driver'>org.h2.Driver</entry>
    <entry key='database.url'>jdbc:h2:./data/database</entry>
    <entry key='database.user'>sa</entry>
    <entry key='database.password' />

    <!-- Reenvío de posiciones hacia el backend del host Windows -->
    <entry key='forward.enable'>true</entry>
    <entry key='forward.url'>http://host.docker.internal:8000/api/empresa/gps/traccar/</entry>
    <entry key='forward.type'>json</entry>

</properties>
